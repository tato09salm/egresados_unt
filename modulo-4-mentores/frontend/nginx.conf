server { listen 80; root /usr/share/nginx/html; index index.html;
  location / { try_files $uri $uri/ /index.html; }
  location /api/ { proxy_pass http://modulo4-backend:3004; proxy_http_version 1.1; proxy_set_header Host $host; }
}
